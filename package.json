{
  "name": "hirein",
  "version": "1.0.0",
  "description": "A web application as a medium of communication between employees and employer",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "HireIn Team",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
